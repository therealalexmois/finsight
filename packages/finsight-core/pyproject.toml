[project]
name = "finsight-core"
version = "1.0.0"
description = "Общий код, переиспользуемый сервисами FinSight."
authors = [{ name = "Aleksander Moiseenko", email = "alex.mois.developer@gmail.com" }]
license = "MIT"
requires-python = ">=3.13,<4"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["finsight_core"]
